package com.database.MedScheduler.repository;

import com.database.MedScheduler.models.SetAnalize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SetAnalizeRepository extends JpaRepository<SetAnalize, Integer> {
}
