package com.database.MedScheduler.repository;

import com.database.MedScheduler.models.ZonaSpecialitate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ZonaSpecialitateRepository extends JpaRepository<ZonaSpecialitate, Integer> {
}
